---
"@executor-js/api": patch
"@executor-js/sdk": patch
---

Publish the server-safe typed Executor HTTP client at `@executor-js/api/client` together with the
SDK's shared connection-handoff contracts.
